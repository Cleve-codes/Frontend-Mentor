const Search = () => {
  return (
    <div className="search">
      <input type="text" placeholder="Start typing to search for notes..." />
      <span className="search-icon">🔍️</span>
    </div>
  );
};

export default Search;
